# Suppress audit warnings for transitive dependencies we can't control
audit=false

# Use legacy peer deps to avoid conflicts with react-scripts
legacy-peer-deps=true

# Optimize installation
prefer-offline=true
